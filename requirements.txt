# Core scientific stack
# Install these via conda to avoid source-compilation issues:
#   conda install scipy numpy pandas matplotlib
# The versions below are the minimum compatible versions; conda will resolve them.
numpy>=1.26
scipy>=1.13
matplotlib>=3.9
pandas>=2.2

# Uncertainty propagation
uncertainties==3.2.2

# Curve fitting and optimisation
lmfit==1.3.2

# MCMC / Bayesian sampling
emcee==3.1.6

# Jupyter / Quarto code execution
jupyter==1.1.1
jupyter-cache==1.0.0
nbformat==5.10.4
ipykernel==6.29.5

# Plotting extras
corner==2.2.2        # triangle plots for MCMC posteriors
